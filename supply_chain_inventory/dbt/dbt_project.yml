name: supply_chain_inventory
version: "1.0.0"
config-version: 2

profile: supply_chain_inventory

model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  supply_chain_inventory:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
